#!lua

--[[
  GLSLang Project Description
]]--

table.insert(
  projects,
  {
    name     = "GLSLang",
    kind     = "StaticLib",
    language = "C++11",
    location = make.get_proj_root(),

    src_files = {
      make.add_headers(make.get_proj_root() .. "../include/"),
      make.add_headers(make.get_proj_root() .. "../src/"),
      --
      make.add_src(make.get_proj_root() .. "../src/"),
      --
      make.add_doc(make.get_proj_root() .. "../include/"),
      make.add_doc(make.get_proj_root() .. "../src/"),
    },

    src_files_exclude_windows = {
      make.get_proj_root() .. "../src/OSDependent/Unix/**",
    },

    src_files_exclude_linux = {
      make.get_proj_root() .. "../src/OSDependent/Windows/**",
    },

    src_files_exclude_macos = {
      make.get_proj_root() .. "../src/OSDependent/Windows/**",
    },

    public_inc_dirs = {
      make.get_proj_root() .. "../include/",
    },

    private_inc_dirs = {
      make.get_proj_root() .. "../include/glslang/",
      make.get_proj_root() .. "../",
    },

    project_dependencies = {

    },
  }
)
