
task :ci do |t, args|

  Dir.chdir("scripts/solution/")
  sh("../../premake5 --file=solution.lua gmake")
  sh("make all config=release CXX=g++-5")

  Dir.chdir("../../output/release")
  sh("./#{ENV['EXE']}")

end
