#!lua

--[[

  Nau Test Project Description

]]--

table.insert(
  projects,
  {
    name     = "NauUnitTests",
    kind     = "ConsoleApp",
    language = "C++",
    location = make.get_proj_root(),

    lang_settings = {
      exceptions  = true,
      rtti        = true,
    },

    src_files = {
      make.add_headers(make.get_proj_root() .. "../test/"),
      make.add_src(make.get_proj_root() .. "../test/"),
      make.add_doc(make.get_proj_root() .. "../test/"),
    },

    public_inc_dirs = {
      make.get_proj_root() .. "./",
    },

    project_dependencies = {

      -- First Party --

      "Nau",

      -- Third Party --

      "Catch",
    },
  }
)

table.insert(
  projects,
  {
    name     = "NauFuncTests",
    kind     = "ConsoleApp",
    language = "C++",
    location = make.get_proj_root(),

    lang_settings = {
      exceptions  = true,
      rtti        = true,
    },

    src_files = {
      make.add_headers(make.get_proj_root() .. "../tests/func"),
      make.add_src(make.get_proj_root() .. "../tests/func"),
      make.add_doc(make.get_proj_root() .. "../tests/func"),
    },

    public_inc_dirs = {
      make.get_proj_root() .. "./",
    },

    project_dependencies = {

      -- First Party --

      "Nau",

      -- Third Party --

      "GLFW",
      "OpenGL",
    },
  }
)
