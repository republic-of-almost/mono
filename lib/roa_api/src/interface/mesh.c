#include <rep/rep_api.h>
#include <roa_lib/assert.h>


void
rep_mesh_find(
  uint64_t * out_mesh_ids,
  const char ** mesh_names,
  unsigned mesh_name_count)
{
  ROA_ASSERT(out_mesh_ids);
  ROA_ASSERT(mesh_names);
  ROA_ASSERT(mesh_name_count);

}