#!lua

--[[

  Codex Unit Test Project Description

]]--

table.insert(
  projects,
  {
    name     = "CodexUnitTests",
    kind     = "ConsoleApp",
    language = "C++",
    location = make.get_proj_root(),

    lang_settings = {
      exceptions  = true,
      rtti        = true,
    },

    src_files = {
      make.add_src(make.get_proj_root() .. "../test/unit"),
      make.add_doc(make.get_proj_root() .. "../test/unit"),
    },

    project_dependencies = {

      -- First Party --

      "Codex",

      -- Third Party --

      "Catch",
    },
  }
)
